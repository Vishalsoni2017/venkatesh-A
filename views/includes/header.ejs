<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,300i,400,400i,700,700i" rel="stylesheet">
    <link rel="stylesheet" href="/main.css">
    <title>DevSpace</title>
</head>
<body>
    
    <header class="header-bar">
        <div class="container d-flex flex-column flex-md-row p-3 align-items-center">
            <h4 class="mr-md-auto my-0 font-weight-bold mb-3 mb-md-0"><a href="/" class="text-white"> DevSpace</a></h4>
           <% if(user){ %>
            <div class="flew-row my-3 my-md-3">
                <a href="#" class="text-white mr-2 header-search-icon" title="Search" data-toggle="tooltip" data-placement="bottom"><i class='fas fa-search'></i></a>
                <span class="text-white mr-2 header-chat-icon" title="chat" data-toggle="tooltip" data-placement="bottom"><i class="fas fa-comment"></i></span>
                <a href="/profile/<%= user.username %>" class="mr-2"><img src="<%= user.avatar %>" style="width:32px;height: 32px;border-radius:16px" data-placement="bottom" data-toggle="tooltip" title="My Profile" alt=""></a>
                <a class="btn btn-sm btn-success mr-2" href="/create-post">Create Post</a>
                <form action="/logout" method="post" class="d-inline">
                    <button class="btn btn-sm btn-secondary">Sign Out</button>
                </form>
                
            </div>

           <% }else{ %>
            <form action="/login" method="post">
                <div class="row align-items-center">
                    <div class="col-md mb-3 mb-md-0">
                        <input type="text" name="username" placeholder="Username" class="form-control form-control-sm input-dark">
                    </div>
                    <div class="col-md mb-3 mb-md-0">
                        <input type="password" name="password" placeholder="Password" class="form-control form-control-sm input-dark">
                    </div>
                    <div class="col-md-auto">
                        <button class="btn btn-primary btn-sm">Log In</button>
                    </div>
                </div>
            </form>

         <%  } %>
            
            
            
        </div>

    </header>